<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="up.info.ihm">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.WelcomeScreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.DarkLightMode" />
        <activity android:name=".activities.ParametresLanguage" />
        <activity android:name=".activities.ParametresCodePin" />
        <activity android:name=".activities.Parametres" />
        <activity
            android:name=".activities.CreateCodePin"
            android:noHistory="true" />
        <activity
            android:name=".activities.AppPresentation"
            android:noHistory="true" />
        <activity
            android:name=".activities.Login"
            android:noHistory="true" />
        <activity
            android:name=".activities.AddTaskActivity"
            android:theme="@style/TaskDetails" />
        <activity
            android:name=".activities.TaskDetailsActivity"
            android:theme="@style/TaskDetails" />
        <activity android:name=".activities.MainActivity" />
    </application>
</manifest>